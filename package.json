{
  "name": "devchallenges-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass": "node-sass -w scss/ -o css/ --recursive"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  },
  "dependencies": {
    "node-sass": "^8.0.0"
  }
}
